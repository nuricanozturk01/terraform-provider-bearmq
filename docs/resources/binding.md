---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "bearmq_binding Resource - bearmq"
subcategory: ""
description: |-
  A binding from a source exchange to a queue or exchange. Immutable — every argument forces replacement.
---

# bearmq_binding (Resource)

A binding from a source exchange to a queue or exchange. Immutable — every argument forces replacement.

## Example Usage

```terraform
resource "bearmq_vhost" "orders" {
  name = "orders-prod"
}

resource "bearmq_exchange" "orders" {
  vhost_id = bearmq_vhost.orders.id
  name     = "orders"
  type     = "TOPIC"
}

resource "bearmq_queue" "paid" {
  vhost_id = bearmq_vhost.orders.id
  name     = "orders.paid"
}

# Route "order.paid" messages from the exchange into the queue.
resource "bearmq_binding" "paid" {
  vhost_id         = bearmq_vhost.orders.id
  source           = bearmq_exchange.orders.name
  destination      = bearmq_queue.paid.name
  destination_type = "QUEUE"
  routing_key      = "order.paid"
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `destination` (String) Name of the destination queue or exchange. Must already exist.
- `destination_type` (String) `QUEUE` or `EXCHANGE` (case-insensitive).
- `source` (String) Name of the source exchange. Must already exist in the virtual host.
- `vhost_id` (String) ID of the owning virtual host.

### Optional

- `arguments` (Map of String) Binding arguments (used by headers exchanges). Not refreshed from the server.
- `routing_key` (String) Routing key / binding pattern. Defaults to empty.

### Read-Only

- `id` (String) The ID of this resource.
- `status` (String) Lifecycle status reported by BearMQ.

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
# Bindings import as "<vhost_id>/<binding_id>".
terraform import bearmq_binding.paid 7f3c1e90-0c2a-4b1d-9f4e-1a2b3c4d5e6f/c3d4e5f6-a7b8-9012-cdef-0123456789ab
```
