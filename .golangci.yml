run:
  timeout: 5m

linters:
  enable:
    - govet
    - errcheck
    - staticcheck
    - unused
    - ineffassign
    - misspell
    - revive
    - gofmt
    - goimports
    - unconvert
    - bodyclose

issues:
  exclude-rules:
    - path: _test\.go
      linters:
        - errcheck
